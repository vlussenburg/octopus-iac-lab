apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sReplicasPerTier
metadata:
  name: deployments-tier-replica-cap
spec:
  enforcementAction: deny
  match:
    kinds:
      - apiGroups: ["apps"]
        kinds: ["Deployment"]
