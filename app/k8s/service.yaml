apiVersion: v1
kind: Service
metadata:
  name: randomquotes
  namespace: randomquotes
spec:
  type: LoadBalancer
  selector:
    app: randomquotes
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
